[tox]
envlist = 3.7, 3.8, 3.9, 3.10
isolated_build = True
skipsdist = True

[testenv]
commands = 
    pytest
deps = 
    pyyaml
    requests
    click
    rich
    pytest
    python-dotenv
passenv =
    GH_TOKEN